[package]
name = "rpc_example"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
env_logger = "0.10"
livekit = { workspace = true, features = ["native-tls"]}
livekit-api = { workspace = true }
log = "0.4"
rand = "0.8"
serde_json = "1.0"
