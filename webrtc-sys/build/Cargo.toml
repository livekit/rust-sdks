[package]
name = "webrtc-sys-build"
version = "0.1.3"
edition = "2021"

[dependencies]
reqwest = { version = "0.11", features = ["blocking", "json", "rustls-tls"], default-features = false }
zip = "0.6.3"
regex = "1.0"
scratch = "1.0"
fs2 = "0.4.3"
