<<<<<<< HEAD
[package]
name = "livekit-a2a-relay"
version = "0.1.0"
edition = "2021"
description = "Ultra-Low Latency Agent-to-Agent (A2A) Relay for LiveKit"
license = "Apache-2.0"

[dependencies]
# During development and verification, we reference the fork's branch to use the protocol fixes.
# Once upstream has merged the protocol fix PR, this can be changed to point to the upstream repo:
# livekit = { git = "https://github.com/livekit/rust-sdks.git" }
# Or to the crates.io release once version 0.7.45+ is published.
livekit = { path = "../livekit" }

tokio = { version = "1", features = ["sync", "rt", "time"] }
log = "0.4"
futures-util = { version = "0.3", default-features = false, features = ["sink"] }
thiserror = "2"
uuid = { version = "1", features = ["v4"], optional = true }
a2a-client = { version = "0.2.0", optional = true }
a2a-slimrpc = { version = "0.1.13", optional = true }

[features]
default = []
# Enables the OfficialA2aClient backed by the upstream a2a-client crate (HTTP/JSON-RPC).
a2a-client = ["dep:a2a-client", "dep:uuid"]
# Enables both a2a-client and the SLIM-RPC transport (work-in-progress).
a2a-integration = ["a2a-client", "dep:a2a-slimrpc"]

[dev-dependencies]
# Used by integration tests to run an in-process mock A2A HTTP server.
axum = "0.8"
tokio = { version = "1", features = ["full"] }
uuid = { version = "1", features = ["v4"] }
serde_json = "1"

# Isolate from the parent directory's workspace for standalone builds.
[workspace]
=======
[workspace]
resolver = "2"
members = [
    "livekit",
    "livekit-api",
    "livekit-protocol",
    "livekit-ffi",
    "livekit-uniffi",
    "livekit-datatrack",
    "livekit-ffi-node-bindings",
    "livekit-runtime",
    "livekit-wakeword",
    "libwebrtc",
    "soxr-sys",
    "yuv-sys",
    "imgproc",
    "device-info",
    "webrtc-sys",
    "webrtc-sys/build",

    "examples/agent_dispatch",
    "examples/api",
    "examples/basic_data_track",
    "examples/basic_room",
    "examples/basic_text_stream",
    "examples/data_track_benchmark",
    "examples/encrypted_text_stream",
    "examples/local_audio",
    "examples/local_video",
    "examples/mobile",
    "examples/play_from_disk",
    "examples/rpc",
    "examples/save_to_disk",
    "examples/screensharing",
    "examples/send_bytes",
    "examples/webhooks",
    "examples/wgpu_room",
]

[workspace.package]
edition = "2021"
homepage = "https://livekit.io"
repository = "https://github.com/livekit/rust-sdks"
license = "Apache-2.0"

[workspace.dependencies]
device-info = { version = "0.1.1", path = "device-info" }
imgproc = { version = "0.3.19", path = "imgproc" }
libwebrtc = { version = "0.3.39", path = "libwebrtc" }
livekit = { version = "0.7.50", path = "livekit" }
livekit-api = { version = "0.5.4", path = "livekit-api" }
livekit-ffi = { version = "0.12.68", path = "livekit-ffi" }
livekit-datatrack = { version = "0.1.9", path = "livekit-datatrack" }
livekit-protocol = { version = "0.7.10", path = "livekit-protocol" }
livekit-runtime = { version = "0.4.0", path = "livekit-runtime" }
soxr-sys = { version = "0.1.3", path = "soxr-sys" }
webrtc-sys = { version = "0.3.36", path = "webrtc-sys" }
webrtc-sys-build = { version = "0.3.18", path = "webrtc-sys/build" }
yuv-sys = { version = "0.3.14", path = "yuv-sys" }

anyhow = "1.0"
bytes = "1.10"
clap = "4.5"
console-subscriber = "0.1"
env_logger = "0.11"
from_variants = "1.0.2"
futures = "0.3"
futures-core = "0.3"
futures-util = { version = "0.3", default-features = false }
lazy_static = "1.4"
log = "0.4"
parking_lot = "0.12"
prost = "0.14"
prost-build = "0.14"
prost-types = "0.14"
rand = "0.9"
serde = "1"
serde_json = "1.0"
thiserror = "2"
tokio = { version = "1", default-features = false }
tokio-stream = "0.1"
hound = "3.5"

# For examples
eframe = { version = "0.35.0", default-features = false }
egui = "0.35.0"
egui-wgpu = "0.35.0"
image = "0.25"
wgpu = "29.0"
winit = "0.30.13"
cc = { version = "1.0", features = ["parallel"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"
debug = false
>>>>>>> ef0b36a8472308828310d7eb646a93752cb0a821
