---
---

Implement a lightweight, high-performance, and streaming A2A currency conversion agent in Rust (under `examples/a2a_mock_agent`) to replace the Python FastAPI agent. It supports local offline regex-based parsing of conversion queries and optionally falls back to local Ollama.
