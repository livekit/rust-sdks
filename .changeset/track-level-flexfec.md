---
livekit: minor
---

Add a per-track FlexFEC publish option and room-level protection presets, using a one-frame protection window by default for low-latency applications while keeping advanced frame-window and mask settings internal.
