# livekit-capture

Capture helpers for publishing decoded, DMA-BUF, and pre-encoded video frames
with the LiveKit Rust SDK.

Optional source features include `avfoundation`, `libargus`, `v4l`,
`tcp-source`, `rtsp`, and `gstreamer`.
